{
  "name": "db-perf-api",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "dev": "TS_NODE_COMPILER_OPTIONS='{\"module\":\"commonjs\"}' ts-node-dev --respawn --transpile-only --poll --clear -- src/index.ts"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4",
    "@types/pg": "^8.11.6"
  }
}